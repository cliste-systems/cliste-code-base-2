<?xml version="1.0" encoding="UTF-8"?>
<!--
  Paste into Twilio: Console → TwiML Bins → Create new TwiML Bin.

  Then: Phone Numbers → +353… → Voice → "A call comes in" → TwiML Bin → select this bin → Save.

  SIP host must match LiveKit Cloud → Project settings → SIP (NOT the wss hostname).
  This project uses the UK SIP domain, e.g.
  4crcl51h1rh.uk.sip.livekit.cloud
  (see Twilio call "To" field — use that exact host from the LiveKit dashboard).

  If your inbound trunk uses digest auth, add username/password on <Sip> and
  the same values on the LiveKit inbound trunk (lk sip inbound update …).

  Docs: https://docs.livekit.io/telephony/accepting-calls/inbound-twilio/
-->
<Response>
  <Dial timeout="60" answerOnBridge="true">
    <Sip>
      sip:+353749389378@4crcl51h1rh.uk.sip.livekit.cloud;transport=tcp
    </Sip>
  </Dial>
</Response>
